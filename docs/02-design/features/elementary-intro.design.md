# Design: elementary-intro (초등부 소개 메뉴)

## 개요
- **기능명**: elementary-intro
- **작성일**: 2026-02-19
- **참조 플랜**: `docs/01-plan/features/elementary-intro.plan.md`
- **구현 전략**: 정적 서버 컴포넌트 (MVP) — API 호출 없음

---

## 1. 파일 구조

```
src/
├── app/(dashboard)/
│   └── intro/
│       └── page.tsx              # [신규] 서버 컴포넌트 - 소개 페이지
└── components/
    └── layout/
        └── Sidebar.tsx           # [수정] 메뉴 항목 추가
```

---

## 2. 컴포넌트 설계

### 2-1. `src/app/(dashboard)/intro/page.tsx`

**타입**: Server Component (정적, 데이터 페칭 없음)

```typescript
// 페이지 구조 (의사코드)
export default function IntroPage() {
  return (
    <div className="max-w-4xl mx-auto">
      <Header />
      <HeroBanner />       // 히어로 그라데이션 배너
      <VisionSection />    // 비전 카드
      <GoalsSection />     // 교육 목표 4개 그리드
      <PrinciplesSection /> // 교육 방침 리스트
      <StaffSection />     // 담당자 소개
    </div>
  );
}
```

**섹션별 상세 설계:**

#### 섹션 1: 히어로 배너
```
┌─────────────────────────────────────────┐
│  🦁  동은교회 초등부                      │
│      말씀으로 자라는 하나님의 자녀들        │
│                                         │
│  [ 단체 사진 또는 일러스트 배경 영역 ]     │
└─────────────────────────────────────────┘
```
- `bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500`
- 흰색 텍스트, 반투명 카드 내부 배치
- 패딩: `py-12 px-8`
- 애니메이션: `motion.div` fade-in + slide-up

#### 섹션 2: 비전 카드
```
┌─────────────────────────────────────────┐
│  ✨ 우리의 비전                           │
│                                         │
│  "하나님의 말씀으로 양육받아              │
│   예수님을 닮아가는 어린이"               │
└─────────────────────────────────────────┘
```
- `Card` 컴포넌트 사용 (글래스모피즘)
- 비전 문구: `text-2xl font-bold text-indigo-700` + 이탤릭
- 왼쪽 보더: `border-l-4 border-indigo-500`

#### 섹션 3: 교육 목표 (4개 카드 그리드)
```
┌──────────┬──────────┬──────────┬──────────┐
│ 🙏 예배   │ 📖 말씀   │ 📢 전도   │ 🤝 섬김   │
│          │          │          │          │
│ 온 마음으 │ 말씀을   │ 복음을   │ 이웃을   │
│ 로 예배   │ 암송하는 │ 전하는   │ 섬기는   │
└──────────┴──────────┴──────────┴──────────┘
```
- Grid: `grid grid-cols-2 md:grid-cols-4 gap-4`
- 각 카드: 아이콘 + 제목 + 설명
- 카드 색상: indigo / purple / pink / amber (각각 다른 그라데이션)
- `staggerChildren` 애니메이션

**교육 목표 데이터:**
```typescript
const goals = [
  {
    emoji: '🙏',
    title: '예배',
    description: '온 마음으로 하나님께 예배드리는 어린이',
    color: 'from-indigo-400 to-indigo-600',
  },
  {
    emoji: '📖',
    title: '말씀',
    description: '하나님의 말씀을 읽고 암송하는 어린이',
    color: 'from-purple-400 to-purple-600',
  },
  {
    emoji: '📢',
    title: '전도',
    description: '친구들에게 복음을 전하는 어린이',
    color: 'from-pink-400 to-pink-600',
  },
  {
    emoji: '🤝',
    title: '섬김',
    description: '이웃을 사랑하고 섬기는 어린이',
    color: 'from-amber-400 to-amber-600',
  },
];
```

#### 섹션 4: 교육 방침
```
┌─────────────────────────────────────────┐
│  📋 교육 방침                             │
│                                         │
│  ✅  주일 예배 중심의 신앙 교육             │
│  ✅  가정과 연계한 말씀 생활화              │
│  ✅  소그룹 활동을 통한 공동체 훈련         │
│  ✅  말씀 암송 및 성경 읽기 장려            │
└─────────────────────────────────────────┘
```
- 세로 리스트, 각 항목에 체크 아이콘 (`CheckCircle2` lucide)
- 호버 시 배경 색상 전환 애니메이션

**교육 방침 데이터:**
```typescript
const principles = [
  '주일 예배 중심의 신앙 교육',
  '가정과 연계한 말씀 생활화',
  '소그룹 활동을 통한 공동체 훈련',
  '말씀 암송 및 성경 읽기 장려',
  '절기별 행사와 전도 활동',
];
```

#### 섹션 5: 담당자 소개
```
┌─────────────────────────────────────────┐
│  👥 담당자 소개                            │
│                                         │
│  ┌──────────┐  ┌──────────┐            │
│  │  👤      │  │  👤      │            │
│  │ 담당 교역자│  │ 부장 교사  │            │
│  │ 000 전도사│  │ 000 선생님 │            │
│  └──────────┘  └──────────┘            │
└─────────────────────────────────────────┘
```
- Grid: `grid grid-cols-2 md:grid-cols-3 gap-4`
- 아바타(이니셜) + 이름 + 역할
- 데이터: 하드코딩 (추후 DB 연동 가능)

**담당자 데이터:**
```typescript
const staff = [
  { name: '담당 교역자', role: '교역자', initial: '교', color: 'bg-indigo-100 text-indigo-600' },
  { name: '부장 교사', role: '부장', initial: '부', color: 'bg-purple-100 text-purple-600' },
  { name: '총무 교사', role: '총무', initial: '총', color: 'bg-pink-100 text-pink-600' },
];
```
> 실제 이름은 관리자가 추후 업데이트 예정

---

### 2-2. `src/components/layout/Sidebar.tsx` 수정

**변경 사항**: `navItems` 배열에 항목 추가

```typescript
// 추가할 아이콘
import { BookOpen } from 'lucide-react';

// navItems 배열에 추가 (공지사항 앞 또는 맨 위)
{ href: '/intro', icon: <BookOpen size={20} />, label: '초등부 소개' },
```

**위치**: 기존 출석 관리 앞 (배열 첫 번째) 또는 공지사항 다음
- 결정: 공지사항 바로 앞에 배치 (정보성 메뉴 그룹)
- `hideForParent: false` (학부모도 볼 수 있음)
- `adminOnly: false` (모든 역할 접근 가능)

---

## 3. UI 전체 레이아웃 목업 (모바일 기준)

```
┌─────────────────────────────┐
│  초등부 소개                  │  ← Header 컴포넌트
│  동은교회 초등부를 소개합니다   │
├─────────────────────────────┤
│                             │
│  🦁 동은교회 초등부           │  ← 히어로 (인디고 그라데이션)
│  말씀으로 자라는              │
│  하나님의 자녀들              │
│                             │
├─────────────────────────────┤
│  ✨ 우리의 비전               │  ← 비전 카드
│  "하나님의 말씀으로 양육받아   │
│  예수님을 닮아가는 어린이"     │
├─────────────────────────────┤
│  🎯 교육 목표                 │
│  ┌──────┐  ┌──────┐         │  ← 2x2 그리드 (모바일)
│  │🙏예배│  │📖말씀│         │
│  └──────┘  └──────┘         │
│  ┌──────┐  ┌──────┐         │
│  │📢전도│  │🤝섬김│         │
│  └──────┘  └──────┘         │
├─────────────────────────────┤
│  📋 교육 방침                 │  ← 방침 리스트
│  ✅ 주일 예배 중심의 신앙 교육  │
│  ✅ 가정과 연계한 말씀 생활화   │
│  ...                        │
├─────────────────────────────┤
│  👥 담당자 소개               │  ← 담당자 카드
│  ┌──────┐  ┌──────┐         │
│  │ 교역자│  │ 부장  │         │
│  └──────┘  └──────┘         │
└─────────────────────────────┘
```

---

## 4. 스타일 가이드

| 요소 | 클래스 |
|------|--------|
| 페이지 컨테이너 | `max-w-4xl mx-auto space-y-6` |
| 히어로 배경 | `bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 rounded-3xl` |
| 섹션 제목 | `text-lg font-bold text-gray-800 flex items-center gap-2 mb-4` |
| 목표 카드 | `rounded-2xl p-4 text-white text-center` + 각 그라데이션 |
| 방침 항목 | `flex items-start gap-3 p-3 rounded-xl hover:bg-indigo-50 transition-colors` |
| 담당자 카드 | `Card` 컴포넌트 + 아바타 + 이름 중앙 정렬 |

---

## 5. 애니메이션 설계

```typescript
// 컨테이너: stagger 효과
const containerVariants = {
  hidden: { opacity: 0 },
  show: {
    opacity: 1,
    transition: { staggerChildren: 0.1 }
  }
};

// 각 섹션: fade + slide-up
const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  show: { opacity: 1, y: 0, transition: { duration: 0.4 } }
};
```

---

## 6. 데이터 흐름

```
IntroPage (Server Component)
├── 데이터: 하드코딩 상수 (goals, principles, staff)
├── API 호출: 없음
└── 렌더링: 순수 정적 HTML
```

> **FR-07 (관리자 편집) 구현 시 확장:**
> - `GET /api/intro` → Setting 테이블에서 키-값 읽기
> - `PUT /api/intro` → admin 전용, 내용 업데이트
> - 클라이언트 컴포넌트로 전환

---

## 7. 구현 순서

1. **Sidebar.tsx** 수정 → `BookOpen` 아이콘 import + navItems에 추가
2. **intro/page.tsx** 생성 → 섹션별 순서대로 구현
   - [ ] Header + 히어로 배너
   - [ ] 비전 카드
   - [ ] 교육 목표 그리드
   - [ ] 교육 방침 리스트
   - [ ] 담당자 소개 카드

---

## 8. 수용 기준 (Design 관점)

- [ ] 모든 섹션이 모바일(375px) 및 데스크톱(1280px)에서 올바르게 표시됨
- [ ] 기존 Card, Header UI 컴포넌트 재사용
- [ ] framer-motion stagger 애니메이션 적용
- [ ] `BookOpen` 아이콘으로 사이드바 메뉴 구분
- [ ] 모든 역할(admin/teacher/parent)에서 메뉴 표시 확인
